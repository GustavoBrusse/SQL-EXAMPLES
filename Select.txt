## PROJETO INTRODUÇÃO A LINGUAGEM SQL 

SELECT CUSTOMER_ID, NAME FROM CUSTOMER;
SELECT * FROM PRODUCT;
SELECT
PRODUCT_ID,
DESCRIPTION,
PRICE AS UNTAXED_PRICE,
ROUND(PRICE*1.07,2) AS TAXED_PRICE
FROM PRODUCT;